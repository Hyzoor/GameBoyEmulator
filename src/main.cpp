#include <iostream>
#include <fstream>
#include "definitions.h"
#include "instructionSet.h"

int main(){
    initializeInstructionSet();
    // std::cout << "Hello, from GameBoyEmu!\n";

    // std::ifstream file("../resources/instructionSet.json");
    // Json data = Json::parse(file);
    // data = data["unprefixed"];
    // int numero = data["0x00"]["cycles"].at(0);
    // std::cout << numero;

    

    return 0;
}


